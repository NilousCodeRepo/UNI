TODO: guarda classroom
def trova_nonalpha(testo:str)->set[str]:


def rimpiazza_con_spazi(testo:str, nonalpha:str) ->str

def leggi(file,enc):
    with open(file, enc) as A:
        testo = A.read()
    testo = test.lower()
    nonalpha = trova_nonalpha(testo)
    testo = rimpiazza_con_spazi()
